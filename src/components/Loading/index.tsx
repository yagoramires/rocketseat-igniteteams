import { Container, LoadIndicator } from './styles';
import React from 'react';

const Loading = () => {
  return (
    <Container>
      <LoadIndicator />
    </Container>
  );
};

export default Loading;
